<Window x:Class="S3Browser.TabularFileViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Tabular File Viewer" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="FileNameTextBlock"
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="10"
                   TextWrapping="Wrap"/>

        <Grid Grid.Row="1" Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0"
                      x:Name="HasHeaderCheckBox"
                      Content="File has header"
                      VerticalAlignment="Center"
                      Margin="0,0,20,0"
                      IsChecked="False"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Limit: " 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="RowLimitComboBox"
                          Width="150"
                          SelectedIndex="0">
                    <ComboBoxItem Content="10 rows" Tag="10"/>
                    <ComboBoxItem Content="100 rows" Tag="100"/>
                    <ComboBoxItem Content="1,000 rows" Tag="1000"/>
                    <ComboBoxItem Content="10,000 rows" Tag="10000"/>
                    <ComboBoxItem Content="100,000 rows" Tag="100000"/>
                    <ComboBoxItem Content="All rows" Tag="-1"/>
                </ComboBox>
            </StackPanel>

            <Button Grid.Column="3"
                    Content="Reload"
                    Width="100"
                    Height="30"
                    Click="ReloadButton_Click"/>
        </Grid>

        <Border Grid.Row="2" 
                BorderBrush="Gray" 
                BorderThickness="1" 
                Margin="10,0,10,10">
            <Grid>
                <DataGrid x:Name="ResultsDataGrid"
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          AlternatingRowBackground="WhiteSmoke"
                          GridLinesVisibility="All"
                          HeadersVisibility="All"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"/>
                
                <!-- Loading Overlay -->
                <Grid x:Name="LoadingOverlay" 
                      Background="#CC000000" 
                      Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" 
                                     Width="200" 
                                     Height="20" 
                                     Margin="0,0,0,10"/>
                        <TextBlock x:Name="LoadingMessageTextBlock"
                                   Text="Loading data..." 
                                   Foreground="White" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="10">
            <TextBlock x:Name="StatusTextBlock"
                       Text="Ready to load data"
                       Foreground="Gray"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"/>
            <Button Content="Close" 
                    Width="80" 
                    Height="30"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
