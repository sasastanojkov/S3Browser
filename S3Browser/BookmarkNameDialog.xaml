<Window x:Class="S3Browser.BookmarkNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add Bookmark" 
        Height="220" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Save Bookmark" 
                   FontSize="16" 
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" 
                   Text="Bookmark Name:" 
                   Margin="0,0,0,5"/>
        
        <TextBox Grid.Row="2" 
                 x:Name="BookmarkNameTextBox"
                 Padding="8"
                 FontSize="12"
                 KeyDown="BookmarkNameTextBox_KeyDown"/>

        <TextBlock Grid.Row="3" 
                   x:Name="S3PathTextBlock"
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="11"
                   Margin="0,10,0,0"
                   VerticalAlignment="Top"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button Content="Cancel" 
                    Width="80" 
                    Height="32"
                    IsCancel="True"
                    Click="Cancel_Click" 
                    Margin="0,0,10,0"/>
            <Button Content="Save" 
                    Width="80" 
                    Height="32"
                    IsDefault="True"
                    Click="Save_Click"
                    Background="#FF4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
