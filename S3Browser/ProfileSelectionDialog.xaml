<Window x:Class="S3Browser.ProfileSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select AWS Profile" 
        Height="250" 
        Width="412"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20,20,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto" MinHeight="36.14"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="AWS Profile Selection" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <TextBlock 
                   Text="Please enter the AWS profile name to use:" 
                   Margin="0,31,0,10" Grid.RowSpan="2"/>

        <TextBox Grid.Row="1" 
                 x:Name="ProfileTextBox"
                 Text=""
                 Padding="5"
                 FontSize="14"
                 Margin="0,26,0,10" Grid.RowSpan="2"/>

        <TextBlock Grid.Row="3" 
                   Text="Make sure you have run 'aws sso login --profile [profile-name]' before continuing."
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="11"
                   Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Left" Margin="190,0,0,0" Width="170">
            <Button Content="OK" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
